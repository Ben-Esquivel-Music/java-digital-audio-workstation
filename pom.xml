<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.benesquivelmusic</groupId>
    <artifactId>daw-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>Digital Audio Workstation</name>
    <description>A state-of-the-art Digital Audio Workstation built with JavaFX and Java 26</description>
    <url>https://github.com/Ben-Esquivel-Music/java-digital-audio-workstation</url>

    <licenses>
        <license>
            <name>GPL License</name>
            <url>https://opensource.org/licenses/GPL</url>
        </license>
    </licenses>

    <modules>
        <module>daw-sdk</module>
        <module>daw-core</module>
        <module>daw-acoustics</module>
        <module>daw-fx</module>
        <module>daw-app</module>
    </modules>

    <distributionManagement>
        <repository>
            <id>github</id>
            <name>GitHub Packages</name>
            <url>https://maven.pkg.github.com/Ben-Esquivel-Music/java-digital-audio-workstation</url>
        </repository>
    </distributionManagement>

    <properties>
        <!-- Java version -->
        <java.version>26</java.version>
        <maven.compiler.release>${java.version}</maven.compiler.release>

        <!-- Encoding -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- JavaFX version -->
        <javafx.version>26</javafx.version>

        <!-- Plugin versions -->
        <maven-compiler-plugin.version>3.15.0</maven-compiler-plugin.version>
        <maven-surefire-plugin.version>3.5.5</maven-surefire-plugin.version>
        <maven-failsafe-plugin.version>3.5.5</maven-failsafe-plugin.version>
        <maven-jar-plugin.version>3.5.0</maven-jar-plugin.version>
        <maven-source-plugin.version>3.4.0</maven-source-plugin.version>
        <maven-javadoc-plugin.version>3.12.0</maven-javadoc-plugin.version>
        <javafx-maven-plugin.version>0.0.8</javafx-maven-plugin.version>

        <!-- Test dependency versions -->
        <junit.version>6.0.3</junit.version>
        <assertj.version>3.27.7</assertj.version>
        <mockito.version>5.23.0</mockito.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Internal modules -->
            <dependency>
                <groupId>com.benesquivelmusic</groupId>
                <artifactId>daw-sdk</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.benesquivelmusic</groupId>
                <artifactId>daw-core</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.benesquivelmusic</groupId>
                <artifactId>daw-acoustics</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.benesquivelmusic</groupId>
                <artifactId>daw-fx</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- JavaFX -->
            <dependency>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-controls</artifactId>
                <version>${javafx.version}</version>
            </dependency>
            <dependency>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-graphics</artifactId>
                <version>${javafx.version}</version>
            </dependency>
            <dependency>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-fxml</artifactId>
                <version>${javafx.version}</version>
            </dependency>
            <dependency>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-media</artifactId>
                <version>${javafx.version}</version>
            </dependency>

            <!-- Testing -->
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${junit.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.assertj</groupId>
                <artifactId>assertj-core</artifactId>
                <version>${assertj.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-core</artifactId>
                <version>${mockito.version}</version>
                <scope>test</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Common test dependencies for all modules -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                    <configuration>
                        <release>${java.version}</release>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven-surefire-plugin.version}</version>
					<configuration>
						<!-- Native (FFM / JEP 454) access. Every project module
						     is now a named JPMS module, so ALL-UNNAMED no longer
						     covers them: grant it to daw.core, which owns the
						     PortAudio/FluidSynth/CLAP/codec bindings. ALL-UNNAMED
						     is kept too for test-only helpers that may still run
						     on the unnamed classpath (e.g. patched test code
						     before module resolution). Per-module surefire
						     <configuration> blocks (daw-core, daw-app) replace
						     this argLine, so they repeat the flag. -->
						<argLine>--enable-native-access=daw.core,ALL-UNNAMED</argLine>
					</configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <version>${maven-failsafe-plugin.version}</version>
                    <executions>
                        <execution>
                            <goals>
                                <goal>integration-test</goal>
                                <goal>verify</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>${maven-jar-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>${maven-source-plugin.version}</version>
                    <executions>
                        <execution>
                            <id>attach-sources</id>
                            <goals>
                                <goal>jar-no-fork</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>${maven-javadoc-plugin.version}</version>
                    <executions>
                        <execution>
                            <id>attach-javadocs</id>
                            <goals>
                                <goal>jar</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.openjfx</groupId>
                    <artifactId>javafx-maven-plugin</artifactId>
                    <version>${javafx-maven-plugin.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!--
            ─────────────────────────────────────────────────────────────
            long-tests profile (story 209)
            ─────────────────────────────────────────────────────────────
            Long-running end-to-end render / export tests that take
            minutes per test (full master render, 20-track batch export,
            DDP image validation, ADM BWF round-trip, deliverable bundle
            export). Excluded from the default fast feedback loop and
            invoked nightly or on `main` pushes via the long-tests CI
            workflow.

            Per-module activation:
              * daw-app  — adds `src/test/long/java` + `src/test/long/resources`
                           as the test source set (see daw-app/pom.xml).
              * daw-core — enables the strict DSP regression coverage
                           gate (see daw-core/pom.xml).
              * other modules — run their regular unit tests with the
                                longer fork timeout and report directory
                                from this profile; no additional test
                                sources are added.

            Reports land in `target/long-test-reports/` so they don't
            collide with the standard surefire output.

            Run locally with:
                mvn -Plong-tests verify
        -->
        <profile>
            <id>long-tests</id>
            <properties>
                <!-- Set to true when CI is rebaselining golden artifacts. Tests
                     should overwrite golden files instead of comparing. -->
                <longtests.rebaseline>false</longtests.rebaseline>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <reportsDirectory>${project.build.directory}/long-test-reports</reportsDirectory>
                            <!-- Generous fork timeout to accommodate the full
                                 5-track / 20-track / bundle pipelines. Per-test
                                 wall-clock budgets are enforced inside the
                                 LongTestHarness (2× the documented budget). -->
                            <forkedProcessTimeoutInSeconds>1800</forkedProcessTimeoutInSeconds>
                            <systemPropertyVariables>
                                <longtests.rebaseline>${longtests.rebaseline}</longtests.rebaseline>
                            </systemPropertyVariables>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
